{
  "private": true,
  "name": "{{uuid}}",
  "version": "1.0.0",
  "author": "{{author}}",
  "main": "./src/index.js",
  "scripts": {
    "start": "node ./src/index.js -debug {{uuid}}",
    "build": "npx ncc build -m ./src/index.js -o ./plugin"
  },
  "dependencies": {
    "elgato-stream-deck-sdk": "^1.0.0"
  }
}
